{{ if and .TransientPayload (index .TransientPayload "profile_name") }}안녕하세요, {{ index .TransientPayload "profile_name" }} 님,{{ else }}안녕하세요, 고객님,{{ end }}

{{ if and .TransientPayload (or (eq (index .TransientPayload "verify_type") "commit_unity") (eq (index .TransientPayload "verify_type") "commit_lifeai")) }}귀하는 방금 지갑에서 Commit Life Points를 요청하셨습니다. 다음은 Commit 거래를 확인하기 위한 OTP입니다:

{{ .VerificationCode }}

거래 상세 정보:
Commit 수량 (LP): {{ index .TransientPayload "amount" }}

이 OTP를 입력하여 Life Points Commit을 진행해 주세요.

OTP는 {{ .ExpiresInMinutes }}분 동안 유효합니다. 이 OTP를 다른 사람과 공유하지 마세요.

{{ .Tenant }}를 선택해 주셔서 감사합니다! 도움이 필요하시면 support@lifenetwork.ai로 문의해 주세요.

감사합니다,
{{ .Tenant }} 팀
{{ else if and .TransientPayload (eq (index .TransientPayload "verify_type") "withdraw_main_wallet") }}LIFE AI 계정에서{{ if and .TransientPayload (index .TransientPayload "amount") }} {{ index .TransientPayload "amount" }}{{ end }} USDT 출금 요청을 받았습니다. 보안을 위해 아래 일회용 비밀번호(OTP)를 사용하여 이 거래를 확인해 주세요.

{{ .VerificationCode }}

LIFE AI 앱에서 이 OTP를 입력하여 거래를 진행해 주세요.

OTP는 {{ .ExpiresInMinutes }}분 동안 유효합니다. 이 OTP를 다른 사람과 공유하지 마세요.

LIFE AI를 선택해 주셔서 감사합니다! 도움이 필요하시면 support@lifenetwork.ai로 문의해 주세요.

감사합니다,
LIFE AI 지원팀
{{ else if and .TransientPayload (eq (index .TransientPayload "verify_type") "withdraw_unity") }}LIFE Unity 계정에서{{ if and .TransientPayload (index .TransientPayload "amount") }} {{ index .TransientPayload "amount" }}{{ end }} USDT 출금 요청을 받았습니다. 보안을 위해 아래 일회용 비밀번호(OTP)를 사용하여 이 거래를 확인해 주세요.

{{ .VerificationCode }}

LIFE Unity 앱에서 이 OTP를 입력하여 거래를 진행해 주세요.

OTP는 {{ .ExpiresInMinutes }}분 동안 유효합니다. 이 OTP를 다른 사람과 공유하지 마세요.

LIFE Unity를 선택해 주셔서 감사합니다! 도움이 필요하시면 support@lifenetwork.ai로 문의해 주세요.

감사합니다,
LIFE Unity 지원팀
{{ else if and .TransientPayload (eq (index .TransientPayload "verify_type") "transfer_commit_unity") }}Life Unity로 Life Points 전송 요청을 받았습니다. 보안을 위해 아래 일회용 비밀번호(OTP)를 사용하여 이 거래를 확인해 주세요.

{{ .VerificationCode }}

LIFE AI 앱에서 이 OTP를 입력하여 거래를 진행해 주세요.

OTP는 {{ .ExpiresInMinutes }}분 동안 유효합니다. 이 OTP를 다른 사람과 공유하지 마세요.

LIFE AI를 선택해 주셔서 감사합니다! 도움이 필요하시면 support@lifenetwork.ai로 문의해 주세요.

감사합니다,
LIFE AI 지원팀
{{ else if and .TransientPayload (eq (index .TransientPayload "verify_type") "pay_loan_transfer") }}대출 상환 및 전송 요청을 받았습니다. 보안을 위해 아래 일회용 비밀번호(OTP)를 사용하여 이 거래를 확인해 주세요.

{{ .VerificationCode }}

LIFE AI 앱에서 이 OTP를 입력하여 거래를 진행해 주세요.

OTP는 {{ .ExpiresInMinutes }}분 동안 유효합니다. 이 OTP를 다른 사람과 공유하지 마세요.

LIFE AI를 선택해 주셔서 감사합니다! 도움이 필요하시면 support@lifenetwork.ai로 문의해 주세요.

감사합니다,
LIFE AI 지원팀
{{ else if and .TransientPayload (eq (index .TransientPayload "verify_type") "pay_loan") }}대출 상환 요청을 받았습니다. 보안을 위해 아래 일회용 비밀번호(OTP)를 사용하여 이 거래를 확인해 주세요.

{{ .VerificationCode }}

LIFE AI 앱에서 이 OTP를 입력하여 거래를 진행해 주세요.

OTP는 {{ .ExpiresInMinutes }}분 동안 유효합니다. 이 OTP를 다른 사람과 공유하지 마세요.

LIFE AI를 선택해 주셔서 감사합니다! 도움이 필요하시면 support@lifenetwork.ai로 문의해 주세요.

감사합니다,
LIFE AI 지원팀
{{ else if and .TransientPayload (index .TransientPayload "action_description") }}{{ index .TransientPayload "action_description" }} 요청을 받았습니다.

보안을 위해 LIFE AI 앱에서 아래의 일회용 비밀번호(OTP)를 입력하여 확인해 주세요:

{{ .VerificationCode }}

OTP는 {{ .ExpiresInMinutes }}분 동안 유효합니다. 이 OTP를 다른 사람과 공유하지 마세요.

본인이 요청하지 않으셨다면 이 이메일을 무시하시고 즉시 support@lifenetwork.ai로 연락하시거나 비밀번호를 변경해 주세요.

LIFE AI를 선택해 주셔서 감사합니다!

감사합니다,
LIFE AI 팀
{{ else }}{{ .Tenant }}에서 인증 요청을 받았습니다.

보안을 위해 LIFE AI 앱에서 아래의 일회용 비밀번호(OTP)를 입력하여 확인해 주세요:

{{ .VerificationCode }}

OTP는 {{ .ExpiresInMinutes }}분 동안 유효합니다. 이 OTP를 다른 사람과 공유하지 마세요.

본인이 요청하지 않으셨다면 이 이메일을 무시하시고 즉시 support@lifenetwork.ai로 연락하시거나 비밀번호를 변경해 주세요.

LIFE AI를 선택해 주셔서 감사합니다!

감사합니다,
LIFE AI 팀
{{ end }}
