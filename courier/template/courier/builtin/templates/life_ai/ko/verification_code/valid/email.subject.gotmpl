{{ if and .TransientPayload (eq (index .TransientPayload "verify_type") "withdraw_main_wallet") }}{{ if and .TransientPayload (index .TransientPayload "amount") }}LIFE AI {{ index .TransientPayload "amount" }} USDT 출금 OTP{{ else }}LIFE AI USDT 출금 OTP{{ end }}{{ else if and .TransientPayload (eq (index .TransientPayload "verify_type") "withdraw_unity") }}{{ if and .TransientPayload (index .TransientPayload "amount") }}LIFE Unity {{ index .TransientPayload "amount" }} USDT 출금 OTP{{ else }}LIFE Unity USDT 출금 OTP{{ end }}{{ else if and .TransientPayload (or (eq (index .TransientPayload "verify_type") "commit_unity") (eq (index .TransientPayload "verify_type") "commit_lifeai")) }}Commit Life Points 확인용 OTP{{ else if and .TransientPayload (eq (index .TransientPayload "verify_type") "transfer_commit_unity") }}Life Unity로 LP 전송 OTP{{ else if and .TransientPayload (eq (index .TransientPayload "verify_type") "pay_loan_transfer") }}대출 상환 및 전송 OTP{{ else if and .TransientPayload (eq (index .TransientPayload "verify_type") "pay_loan") }}대출 상환 OTP{{ else if and .TransientPayload (index .TransientPayload "subject") }}{{ index .TransientPayload "subject" }}{{ else }}{{ .Tenant }} OTP - {{ .ExpiresInMinutes }}분 후 만료{{ end }}
