<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Xác thực OTP</title>
  </head>
  <body style="margin: 0; padding: 0; background-color: #f4f4f4; font-family: Arial, sans-serif;">
    <table role="presentation" width="100%" cellpadding="0" cellspacing="0" style="background-color: #f4f4f4;">
      <tr>
        <td align="center" style="padding: 20px 0;">
          <table role="presentation" width="600" cellpadding="0" cellspacing="0" style="background-color: #ffffff; border-radius: 8px; overflow: hidden; max-width: 600px;">
            <!-- Header with branding -->
            <tr>
              <td align="center" style="padding: 30px 20px;">
                <img src="https://s3-ap-southeast-1.amazonaws.com/genetica.email-templates/build/assets/images/icons/life-ai.logo.png" alt="LIFE AI" width="60" style="display: block;" />
              </td>
            </tr>
            <!-- Body content -->
            <tr>
              <td style="padding: 30px 40px; color: #333333; font-size: 15px; line-height: 1.6;">
                {{ if and .TransientPayload (index .TransientPayload "profile_name") }}<p style="margin: 0 0 16px;">Kính gửi {{ index .TransientPayload "profile_name" }},</p>{{ else }}<p style="margin: 0 0 16px;">Kính gửi Quý khách hàng,</p>{{ end }}

                {{ if and .TransientPayload (eq (index .TransientPayload "verify_type") "commit_unity") }}
                  <p style="margin: 0 0 16px;">Quý khách vừa thực hiện yêu cầu Commit Life Points từ ví của mình. Dưới đây là mã OTP để xác nhận giao dịch này:</p>
                {{ else if and .TransientPayload (index .TransientPayload "action_description") }}
                  <p style="margin: 0 0 16px;">Chúng tôi vừa nhận được yêu cầu {{ index .TransientPayload "action_description" }}. Vì lý do bảo mật, vui lòng sử dụng Mã xác thực một lần (OTP) bên dưới để xác nhận giao dịch này.</p>
                {{ else }}
                  <p style="margin: 0 0 16px;">Chúng tôi vừa nhận được yêu cầu xác thực từ <strong>{{ .Tenant }}</strong>. Vì lý do bảo mật, vui lòng sử dụng Mã xác thực một lần (OTP) bên dưới để xác nhận.</p>
                {{ end }}

                <p style="text-align: center; font-size: 36px; font-weight: bold; letter-spacing: 6px; color: #1a1a2e; margin: 24px 0; padding: 16px 0; border-top: 1px solid #eeeeee; border-bottom: 1px solid #eeeeee;">
                  {{ .VerificationCode }}
                </p>

                {{ if and .TransientPayload (eq (index .TransientPayload "verify_type") "commit_unity") }}
                  <p style="margin: 0 0 16px;"><strong>📌 Chi tiết giao dịch:</strong></p>
                  <p style="margin: 0 0 16px;">Số lượng Commit (LP): <strong>{{ index .TransientPayload "amount" }}</strong></p>
                  <p style="margin: 0 0 16px;">Vui lòng nhập mã OTP này để tiếp tục giao dịch Commit Life Points của quý khách.</p>
                {{ else }}
                  <p style="margin: 0 0 16px;">Vui lòng nhập mã OTP này trong ứng dụng {{ .Tenant }} để tiếp tục.</p>
                {{ end }}

                <p style="margin: 0 0 16px;">Mã OTP này sẽ có hiệu lực trong <strong>{{ .ExpiresInMinutes }}</strong> phút. Vui lòng không chia sẻ mã này với bất kỳ ai để đảm bảo an toàn cho tài khoản của quý khách.</p>

                <p style="margin: 0 0 16px;">Cảm ơn quý khách đã đồng hành cùng {{ .Tenant }}! Nếu cần hỗ trợ, vui lòng liên hệ chúng tôi qua <a href="mailto:support@lifenetwork.ai" style="color: #4caf50; text-decoration: none;">support@lifenetwork.ai</a>.</p>
              </td>
            </tr>
            <!-- Footer -->
            <tr>
              <td style="padding: 20px 40px; background-color: #fafafa; color: #999999; font-size: 12px; text-align: center; border-top: 1px solid #eeeeee;">
                <p style="margin: 0;">&copy; {{ .Tenant }}. All rights reserved.</p>
              </td>
            </tr>
          </table>
        </td>
      </tr>
    </table>
  </body>
</html>
