{{ if and .TransientPayload (eq (index .TransientPayload "verify_type") "withdraw_main_wallet") }}{{ if and .TransientPayload (index .TransientPayload "amount") }}OTP untuk Penarikan {{ index .TransientPayload "amount" }} USDT di LIFE AI{{ else }}OTP untuk Penarikan USDT di LIFE AI{{ end }}{{ else if and .TransientPayload (eq (index .TransientPayload "verify_type") "withdraw_unity") }}{{ if and .TransientPayload (index .TransientPayload "amount") }}OTP untuk Penarikan {{ index .TransientPayload "amount" }} USDT di LIFE Unity{{ else }}OTP untuk Penarikan USDT di LIFE Unity{{ end }}{{ else if and .TransientPayload (or (eq (index .TransientPayload "verify_type") "commit_unity") (eq (index .TransientPayload "verify_type") "commit_lifeai")) }}OTP untuk Verifikasi Commit Life Points{{ else if and .TransientPayload (eq (index .TransientPayload "verify_type") "transfer_commit_unity") }}OTP untuk Transfer LP ke Life Unity{{ else if and .TransientPayload (eq (index .TransientPayload "verify_type") "pay_loan_transfer") }}OTP untuk Bayar Pinjaman & Transfer{{ else if and .TransientPayload (eq (index .TransientPayload "verify_type") "pay_loan") }}OTP untuk Bayar Pinjaman{{ else if and .TransientPayload (index .TransientPayload "subject") }}{{ index .TransientPayload "subject" }}{{ else }}{{ .Tenant }} OTP - Kedaluwarsa dalam {{ .ExpiresInMinutes }} menit{{ end }}
